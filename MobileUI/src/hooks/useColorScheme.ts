/**
 * Color Scheme Hook
 * 
 * This hook provides access to the device's color scheme (light or dark).
 * It re-exports the useColorScheme hook from react-native.
 */

export { useColorScheme } from 'react-native';
